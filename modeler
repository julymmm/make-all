all:
	test -f venv/bin/activate || virtualenv -p $(shell which python) venv
	. venv/bin/activate ; \
		pip install numpy scipy sklearn ; \
		python model.py \
		  -t ../../data/train_full/ \
		  -u ../../data/test_full/ \
		  -m both \
		  -c 0 \
		  -v > ../../output/full-results.txt
